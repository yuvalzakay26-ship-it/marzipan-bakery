import { productsData } from '../../data/productsData.js'

function ProductGallery() {
  return (
    <div>
      {console.log(productsData.bread.id)}
      {console.log(productsData.bread.name)}
      {console.log(productsData.bread.id)}
      {console.log(productsData.bread.id)}
      {console.log(productsData.bread.id)}
    </div>
  )
}

export default ProductGallery
