-- =====================================================================
-- Product seeding template.
--
-- The marketing SPA still ships products from src/data/productsData.js
-- (legacy_id will bridge the two). To migrate the catalog into Supabase:
--
--   1. For each product in productsData.js, insert a row here.
--   2. Set legacy_id to the existing numeric id so the cart can rehydrate
--      either source during the cutover.
--   3. Once admin parity is proven, remove productsData.js and read the
--      catalog from Supabase via TanStack Query (Phase 3 cutover).
--
-- This file is intentionally short — the actual ~50 products will be
-- generated by a one-off script (see scripts/seed-from-legacy.mjs in
-- the implementation guide).
-- =====================================================================

-- Example row (real seeding script writes ~50 of these):
-- insert into public.products (
--     slug, category_id, name_he, description_he,
--     price_agorot, unit, image_url, kashrut, is_parve,
--     legacy_id, sort_order
-- ) values (
--     'rugelach-chocolate',
--     (select id from public.categories where slug = 'rugelach'),
--     'רוגלך שוקולד',
--     'הרוגלך הקלאסי שלנו, חם מהתנור.',
--     8500,                                    -- ₪85 / kg
--     'kg',
--     'https://cdn.marzipanbakery.com/products/rugelach-chocolate.webp',
--     'בד״ץ העדה החרדית',
--     true,
--     101,
--     10
-- );
